{
  "name": "@detrix/billing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"Add billing tests\""
  },
  "dependencies": {
    "@detrix/blockchain-adapter": "workspace:*",
    "@detrix/billing-core": "workspace:*",
    "@detrix/shared-types": "workspace:*",
    "ethers": "^6.15.0"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
