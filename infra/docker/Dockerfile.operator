FROM node:20-alpine
WORKDIR /app
RUN corepack enable
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile
COPY . .
EXPOSE 3000
RUN addgroup -S app && adduser -S app -G app
USER app
CMD ["pnpm", "--filter", "@detrix/operator", "dev", "--host", "0.0.0.0"]